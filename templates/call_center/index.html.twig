{% extends 'base.html.twig' %}

{% block title %}Bénéficiaires{% endblock %}

{% block body %}
<div class="main">
    {{ include('include/_navigation.html.twig') }}
    <div class="content">
        <div class="container">
            <h4>Les Clients</h4>
            <div class="search-container">
                <div class="search-controleurs">
                    {# {{ form_start(form) }} #}
                    {# {{ form_row(form.name, {'attr': {'class': 'search-container-select'}}) }} #}
                    {# <button class="btn-submit" type="submit"><i class="fa fa-search" aria-hidden="true"></i> #}
                    {# </button> #}
                    {# {{ form_end(form) }} #}
                </div>
            </div>
                <div class="table">
                    <table>
                        <tr class="table-title">
                            <th class="table-title-name paddingleft20px">Client</th>
                            <th class="table-title-date">Adresse mail</th>
                            <th class="table-title-date">Téléphone</th>
                        </tr>
                        {% for c in client %}

                            <tr>
                                <td class="table-name paddingleft20px">

                                     <a href="{{ path('call_center_client',{'id': c.id}) }}">{{ c.name }}</a>
                                </td>

                                <td class="table-date">{{ c.email }}</td>
                                <td class="table-date">{{ c.telephone }}</td>





                            </tr>
                        {% endfor %}
                    </table>
                </div>   </div>
                <div class="pagination">
                    <div class="navigation-knp">
                        {# {{ knp_pagination_render(beneficiaires) }} #}
                    </div>
                </div>


    </div>
    {% endblock %}
    {% block javascripts %}

{% endblock %}
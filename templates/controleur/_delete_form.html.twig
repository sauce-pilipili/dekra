<form method="post" action="{{ path('controleur_delete', {'id': controleur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ controleur.id) }}">
    <button class="btn">Delete</button>
</form>

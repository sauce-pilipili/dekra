{% extends 'base.html.twig' %}

{% block title %}Contrôleurs{% endblock %}

{% block body %}
    <h1>Controleur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ controleur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ controleur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ controleur.prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('controleur_index') }}">back to list</a>

    <a href="{{ path('controleur_edit', {'id': controleur.id}) }}">edit</a>

    {{ include('controleur/_delete_form.html.twig') }}
{% endblock %}
